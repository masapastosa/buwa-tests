package utils

import (
//"errors"
)

/*func IsSliceFull(slice interface{}) bool {
	if len(slice) == cap(slice) {
		return true
	} else {
		return false
	}
}

func SliceInsert(slice interface{}) error {
	if IsSliceFull(slice) {
		return errors.New("slices: can't insert, the slice is full")
	}
	return nil
}*/
